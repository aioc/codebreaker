<!DOCTYPE html>
<html>
	<head>
		<link rel="stylesheet" href="/static/layout.css">
		<meta charset="utf-8">
		<meta http-equiv="refresh" content="10" >
		<title>Queue - Codebreaker</title>
	</head>
	<body>
		<table class="results">
			<thead>
				<tr>
					<th>ID</th>
					<th>User</th>
					<th>Problem</td>
					<th>Score</th>
					<th>Status</th>
					<th>Data</th>
                    <th>Actions</th>
				</tr>
			</thead>
			{% for result in results[::-1] %}
				<tbody>
					<tr>
						<td>{{ result.id }}</td>
						<td>{{ result.owner }}</td>
						<td>{{ result.problem }}</td>
						{% if result.complete == false %}
							<td>?</td>
						{% elif result.score > 0 %}
							<td class="score-100">{{ result.score }}</td>
						{% else %}
							<td class="score-0">{{ result.score }}</td>
						{% endif %}
						<td>{{ result.status }}</td>
						<td><a href="/submission/{{result.id}}" target="_blank">View</a></td>
                        <td><a href="/rejudge/{{result.id}}">Rejudge</a> <a href="/delete/{{result.id}}">Delete</a></td>
					</tr>
				</tbody>
			{% endfor %}
		</table>
	</body>
</html>
