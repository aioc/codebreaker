// If in a textarea, tab will insert a space rather than exiting the box.

/*
document.querySelector('textarea').addEventListener('keydown', function (e) {
	if (e.which == 9) {
		e.preventDefault();
	}
});
*/
